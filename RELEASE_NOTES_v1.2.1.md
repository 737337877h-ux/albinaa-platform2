# v1.2.1 — صفحة الإشعارات الكاملة (Web Hotfix)

## ملخص الإصدار
إصلاح سريع للواجهة على أساس Web Dashboard في v1.2.0: استبدال صفحة الإشعارات
المؤقتة (`/notifications`) بصفحة كاملة تعمل مع الـ API الموجود — بلا أي تغييرات
في الـ Schema أو الـ Migration أو الاعتماديات.

## ما الجديد في v1.2.1

### 1) صفحة إشعارات كاملة
- **قائمة الإشعارات** مع نوع الإشعار (وعد مستحق / وعد متأخر / تحصيل جديد / نقل عميل)
  ووصف تفصيلي (العميل، المبلغ والعملة، التاريخ، السبب).
- **حالة القراءة** — تمييز بصري للإشعارات غير المقروءة.
- **رابط الانتقال** لبطاقة العميل المرتبط عند توفره في الـ payload.
- **ترقيم صفحات** (25 لكل صفحة) عبر `GET /notifications?page=&limit=`.

### 2) إدارة القراءة
- **تحديد كمقروء** — زر لكل إشعار غير مقروء عبر `PATCH /notifications/:id/read`.
- **تحديد الكل كمقروء** — زر في رأس الصفحة عبر `PATCH /notifications/read-all`.
- **فلتر غير المقروءة فقط** مع عدّاد الإشعارات غير المقروءة.

### 3) مزامنة جرس الإشعارات
- عدّاد الجرس في الشريط العلوي يتحدّث فورًا بعد قراءة أي إشعار (invalidate
  لاستعلامي `notifications` و`notifications-preview`).

## النسخة
- Web Dashboard (Next.js 14): **1.2.1**
- Backend (NestJS + Prisma + PostgreSQL): **1.2.1**
- Mobile App: لم يتغير في v1.2.1 (يبقى 1.1.0)

## البنية
- بدون تغييرات في الـSchema أو الـMigration أو الاعتماديات.
- يستخدم حصريًا الـ API الموجود: `GET /notifications` و`PATCH /notifications/:id/read`
  و`PATCH /notifications/read-all`.
- backend/frontend typecheck + lint نظيفان.

## ملاحظات
- النسخ الرسمي/التاريخية موثقة في `CHANGELOG.md` و`PROJECT_STATE.md`.
