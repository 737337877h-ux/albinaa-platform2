-- ============================================================================
-- Migration: 20260719120000_m5_review_fixes  (تصحيحات مراجعة M5)
-- السبب (البند خامسًا من المراجعة): حالة partially_fulfilled يجب أن تحمل
-- المبلغ المنفذ فعليًا كقيمة رقمية، لا كحالة نصية مجردة.
-- fulfilled_amount = إجمالي المنفذ من الوعد (المتبقي = expected - fulfilled).
-- ============================================================================
ALTER TABLE payment_promises
    ADD COLUMN fulfilled_amount NUMERIC(18,4);
