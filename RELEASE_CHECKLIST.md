# قائمة قبول الإصدار — v1.3.0 RC

**آخر تحقق:** 2026-08-06 — فرع `codex/android-offline-lan`، دون دمج أو إنشاء tag.

## الفحوص الآلية

- [x] TypeScript وESLint للخلفية والواجهة والجوال بلا أخطاء.
- [x] بناء NestJS وNext.js وصور Docker للإنتاج.
- [x] Backend: ‏106 اختبارات وحدة ناجحة.
- [x] Backend E2E: ‏165 اختبارًا ضمن 19 حزمة على PostgreSQL 16 جديد مع تطبيق كل migrations.
- [x] Frontend: ‏55 اختبارًا ناجحًا.
- [x] Mobile: ‏46 اختبارًا، وExpo Doctor ‏20/20.
- [x] Parser: ‏34 اختبار Python وفحص بناء اختبار الأداء.
- [x] لا ثغرات إنتاجية معروفة في تبعيات backend/frontend.
- [!] تدقيق mobile يعرض 11 تحذيرًا متوسطًا داخل سلسلة أدوات Expo؛ لا يوجد تحذير عالٍ أو حرج، والإصلاح المقترح آليًا يكسر توافق SDK 57 لذلك لم يُطبّق.
- [x] CI يشغّل PostgreSQL service ثم migrate وseed وE2E.

## وظائف القبول في بيئة التجربة

- [x] تسجيل الدخول والخروج والصلاحيات الإدارية والمحصّل — موثقة في UAT السابق وتغطيها E2E.
- [x] استيراد البيانات الحقيقية وفتح Customer 360 والتحقق من الرصيد والمخاطر والمهام.
- [x] تسجيل تحصيل وعكسه مع فصل المهام وحفظ القيد المضاد — موثق في UAT السابق.
- [x] اختبار النسخ الاحتياطي والاستعادة المعزولة؛ الملف `/backups/full/albinaa_full_20260805_020000.tar.gz` اجتاز التحقق.
- [x] بطاقة +120 ومحرك المخاطر موصولان بمحرك الأعمار: 173 حسابًا +120، و36 مخاطرة مرتفعة في فحص 2026-08-06.
- [x] `/admin/units` يعمل، ومتوسط سعر الطن يظهر لكل عملة في بطاقة الحجوزات.
- [x] PDF وXLSX الإداريان متاحان من الويب؛ تم توليد PDF من بيانات staging وفحص الصفحة بصريًا بعد إصلاح الخطوط المختلطة.
- [ ] تجربة اتصال وSMS وWhatsApp برقم يمني فعلي — تتطلب رقم اختبار وموافقة صاحب الرقم.
- [ ] إنشاء حجز وتعديل حالته يدويًا في جلسة UAT النهائية.
- [ ] تصفية تقرير التحصيل يدويًا حسب الفرع والمحصّل والفترة وتنزيل Excel النهائي.
- [ ] اختبار حساب قراءة فقط — لا يوجد حساب UAT مخصص حتى الآن.
- [ ] اختبار ميداني نهائي لتطبيق Android: شهر دون اتصال، ثم مزامنة محلية، والبصمة والموقع والإشعارات.

## Push على الهاتف

- [x] تسجيل وإلغاء device token للمستخدم، وتخزينه في PostgreSQL.
- [x] إرسال اختياري عبر Expo إلى FCM/APNS، مع حذف `DeviceNotRegistered` وعدم تعطيل العملية الأساسية عند فشل المزود.
- [x] Push معطل افتراضيًا؛ الإشعارات الداخلية والمحلية والعمل دون اتصال لا تعتمد عليه.
- [ ] تفعيل `PUSH_ENABLED=true` بعد إضافة اعتماد Expo/FCM/APNS الحقيقي واختبار هاتف فعلي.

## إعدادات الإنتاج

- [x] تطبيق migrations الجديدة على PostgreSQL منفصل وعلى staging أولًا.
- [x] إضافة فاحص `.env.prod` لمنع القيم الافتراضية والأسرار القصيرة وHTTP/CORS غير الآمن.
- [x] إضافة health monitor لمساري `/health` و`/health/ready` كل 15 دقيقة عند ضبط متغير المستودع.
- [x] خطة التراجع إلى tag `v1.2.1` موثقة في `DEPLOYMENT.md`.
- [ ] إنشاء `.env.prod` حقيقي خارج Git وتوليد أسرار JWT وكلمات مرور نهائية.
- [ ] ضبط النطاق الحقيقي وCORS وشهادة TLS ومتغير `PRODUCTION_HEALTH_URL`.
- [ ] اعتماد تخزين خارجي مشفر للنسخ الاحتياطية واختبار تنبيه health فعلي.

## قرار الإصدار

- [ ] إغلاق بنود UAT اليدوية أعلاه.
- [ ] موافقة مالك المنتج على النتائج وقرار دمج PR.
- [ ] تحديث `CHANGELOG.md` وأرقام الحزم وإنشاء tag `v1.3.0` بعد الموافقة فقط.
